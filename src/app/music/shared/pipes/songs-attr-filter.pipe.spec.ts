import { SongsAtrrFilterPipe } from './songs-attr-filter.pipe';

describe('SongsAtrrFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SongsAtrrFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
