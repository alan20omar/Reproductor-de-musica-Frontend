import { FavoriteSongsFilterPipe } from './favorite-songs-filter.pipe';

describe('FavoriteSongsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteSongsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
