import { AlbumArtistGenreFilterPipe } from './album-artist-genre-filter.pipe';

describe('AlbumArtistGenreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumArtistGenreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
