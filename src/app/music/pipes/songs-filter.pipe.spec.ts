import { SongsFilterPipe } from './songs-filter.pipe';

describe('SongsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SongsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
