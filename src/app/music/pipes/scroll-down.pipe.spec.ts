import { ScrollDownPipe } from './scroll-down.pipe';

describe('ScrollDownPipe', () => {
  it('create an instance', () => {
    const pipe = new ScrollDownPipe();
    expect(pipe).toBeTruthy();
  });
});
